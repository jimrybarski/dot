# fish_complete_path set to 1 enables path completion that autofills all common characters.
set -U fish_complete_path 1
# fish_complete_scope set to 2 ensures that the completion engine uses all common characters in the completions.
set -U fish_complete_scope 2
